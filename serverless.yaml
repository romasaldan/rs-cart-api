service: serverless-rds-service

plugins:
  - 'serverless-plugin-typescript'
  - serverless-plugin-optimize
  - serverless-offline

provider:
  name: aws
  runtime: nodejs16.x
  region: eu-north-1
  iam:
    role: arn:aws:iam::332213976395:role/RDSRole

functions:
  serverWithDatabase:
    handler: dist/main.handler
    events:
      - http:
          method: any
          path: /{any+}
